desc 'Create Google Chrome Extension zip on the Desktop'
task :create_zip do
  puts 'Crate zip file on the Desktop'
  system 'zip ~/Desktop/thl4chrome.zip *'
end

task :default do
  sh 'rake create_zip'
end
